// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Grants a user a permission level on an MLflow experiment. Valid `permission` values are `READ`, `EDIT`, `MANAGE` and `NO_PERMISSIONS`; only `permission` can be updated in place (changing the experiment or user replaces the grant).
 *
 * > **Requires the MLflow auth app.** These resources call the [MLflow authentication REST API](https://mlflow.org/docs/latest/auth/rest-api.html), which is only available when the tracking server runs with the `mlflow.server.auth` app enabled (e.g. `mlflow server --app-name basic-auth`).
 *
 * ## Example Usage
 *
 * ## Import
 *
 * Import an existing resource with its id:
 *
 * ```sh
 * $ pulumi import mlflow:auth:ExperimentPermission example <experimentId>/<username>
 * ```
 */
export class ExperimentPermission extends pulumi.CustomResource {
    /**
     * Get an existing ExperimentPermission resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, opts?: pulumi.CustomResourceOptions): ExperimentPermission {
        return new ExperimentPermission(name, undefined as any, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'mlflow:auth:ExperimentPermission';

    /**
     * Returns true if the given object is an instance of ExperimentPermission.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ExperimentPermission {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ExperimentPermission.__pulumiType;
    }

    /**
     * The experiment id. Changing this forces replacement.
     */
    declare public readonly experimentId: pulumi.Output<string>;
    /**
     * The permission level granted: `READ`, `EDIT`, `MANAGE` or `NO_PERMISSIONS`. Updatable in place.
     */
    declare public readonly permission: pulumi.Output<string>;
    /**
     * The user to grant the permission to. Changing this forces replacement.
     */
    declare public readonly username: pulumi.Output<string>;

    /**
     * Create a ExperimentPermission resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ExperimentPermissionArgs, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (!opts.id) {
            if (args?.experimentId === undefined && !opts.urn) {
                throw new Error("Missing required property 'experimentId'");
            }
            if (args?.permission === undefined && !opts.urn) {
                throw new Error("Missing required property 'permission'");
            }
            if (args?.username === undefined && !opts.urn) {
                throw new Error("Missing required property 'username'");
            }
            resourceInputs["experimentId"] = args?.experimentId;
            resourceInputs["permission"] = args?.permission;
            resourceInputs["username"] = args?.username;
        } else {
            resourceInputs["experimentId"] = undefined /*out*/;
            resourceInputs["permission"] = undefined /*out*/;
            resourceInputs["username"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const replaceOnChanges = { replaceOnChanges: ["experimentId", "username"] };
        opts = pulumi.mergeOptions(opts, replaceOnChanges);
        super(ExperimentPermission.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * The set of arguments for constructing a ExperimentPermission resource.
 */
export interface ExperimentPermissionArgs {
    /**
     * The experiment id. Changing this forces replacement.
     */
    experimentId: pulumi.Input<string>;
    /**
     * The permission level granted: `READ`, `EDIT`, `MANAGE` or `NO_PERMISSIONS`. Updatable in place.
     */
    permission: pulumi.Input<string>;
    /**
     * The user to grant the permission to. Changing this forces replacement.
     */
    username: pulumi.Input<string>;
}
