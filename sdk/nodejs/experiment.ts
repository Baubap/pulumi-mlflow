// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages an MLflow **experiment** — a named container that groups runs and their artifacts.
 *
 * Backed by the MLflow [experiments REST API](https://mlflow.org/docs/latest/api_reference/rest-api.html#create-experiment). Works against MLflow tracking servers 2.x and 3.x.
 *
 * > **Note:** MLflow exposes no `delete-experiment-tag` endpoint, so removing a tag in place is not supported; remove it out of band or recreate the experiment. `artifactLocation` is set once at creation — changing it replaces the experiment.
 *
 * ## Example Usage
 *
 * ## Import
 *
 * An existing experiment can be imported by its server-assigned ID:
 *
 * ```sh
 * pulumi import mlflow:index:Experiment demo <experiment_id>
 * ```
 */
export class Experiment extends pulumi.CustomResource {
    /**
     * Get an existing Experiment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, opts?: pulumi.CustomResourceOptions): Experiment {
        return new Experiment(name, undefined as any, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'mlflow:index:Experiment';

    /**
     * Returns true if the given object is an instance of Experiment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Experiment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Experiment.__pulumiType;
    }

    /**
     * Root artifact storage location for the experiment (e.g. an `s3://` URI). Optional — the server assigns a default when omitted. **Immutable:** set once at creation; changing it forces the experiment to be replaced.
     */
    declare public readonly artifactLocation: pulumi.Output<string | undefined>;
    /**
     * Resolved artifact storage location as reported by the tracking server.
     */
    declare public /*out*/ readonly artifactUri: pulumi.Output<string>;
    /**
     * Server-assigned unique identifier of the experiment.
     */
    declare public /*out*/ readonly experimentId: pulumi.Output<string>;
    /**
     * Lifecycle stage of the experiment: "active" or "deleted".
     */
    declare public /*out*/ readonly lifecycleStage: pulumi.Output<string>;
    /**
     * Human-readable, unique name of the experiment. Must be unique across active experiments on the tracking server; updating it renames the experiment in place.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Key/value tags attached to the experiment. Updated in place via `set-experiment-tag`. **Note:** MLflow has no delete-experiment-tag endpoint, so removing a tag in place is not supported.
     */
    declare public readonly tags: pulumi.Output<{[key: string]: string} | undefined>;

    /**
     * Create a Experiment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ExperimentArgs, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (!opts.id) {
            if (args?.name === undefined && !opts.urn) {
                throw new Error("Missing required property 'name'");
            }
            resourceInputs["artifactLocation"] = args?.artifactLocation;
            resourceInputs["name"] = args?.name;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["artifactUri"] = undefined /*out*/;
            resourceInputs["experimentId"] = undefined /*out*/;
            resourceInputs["lifecycleStage"] = undefined /*out*/;
        } else {
            resourceInputs["artifactLocation"] = undefined /*out*/;
            resourceInputs["artifactUri"] = undefined /*out*/;
            resourceInputs["experimentId"] = undefined /*out*/;
            resourceInputs["lifecycleStage"] = undefined /*out*/;
            resourceInputs["name"] = undefined /*out*/;
            resourceInputs["tags"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const replaceOnChanges = { replaceOnChanges: ["artifactLocation"] };
        opts = pulumi.mergeOptions(opts, replaceOnChanges);
        super(Experiment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * The set of arguments for constructing a Experiment resource.
 */
export interface ExperimentArgs {
    /**
     * Root artifact storage location for the experiment (e.g. an `s3://` URI). Optional — the server assigns a default when omitted. **Immutable:** set once at creation; changing it forces the experiment to be replaced.
     */
    artifactLocation?: pulumi.Input<string | undefined>;
    /**
     * Human-readable, unique name of the experiment. Must be unique across active experiments on the tracking server; updating it renames the experiment in place.
     */
    name: pulumi.Input<string>;
    /**
     * Key/value tags attached to the experiment. Updated in place via `set-experiment-tag`. **Note:** MLflow has no delete-experiment-tag endpoint, so removing a tag in place is not supported.
     */
    tags?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
}
