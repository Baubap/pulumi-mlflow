template: native
provider: mlflow
major-version: 0
providerDefaultBranch: main
publishRegistry: true
providerVersion: github.com/Baubap/pulumi-mlflow/provider/version.Version
parallel: 3
sdkModuleDir: sdk/go/mlflow
languages:
  - nodejs
  - python
  - go
# Community provider: secrets come from plain GitHub repo secrets (NPM_TOKEN,
# PYPI_API_TOKEN), not Pulumi ESC. The release workflow is hand-maintained
# (see .github/workflows/release.yml), not generated from this file.
